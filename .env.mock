# Mock mode environment configuration
# Use with: npm run dev:mock

# Enable mock data mode (bypasses Supabase)
VITE_MOCK_MODE=true

# These are not needed in mock mode but set to prevent any potential errors
VITE_SUPABASE_URL=https://mock.supabase.co
VITE_SUPABASE_ANON_KEY=mock-key
